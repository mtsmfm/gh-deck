[environments]
  [environments.development]
    name = "gh-deck"
    namespace = "default"
    wait = false
    watch = false
    watch-delay = 2
    auto-connect = false
    set=[
      "currentDir=/path/to/gh-deck"
    ]
